{# templates/movie_list/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Mes Listes
{% endblock %}

{% block body %}
	<div
		class="container py-5">

		{# En-tête de page #}
		<div class="d-flex justify-content-between align-items-center mb-5 border-bottom pb-3">
			<div>
				<h1 class="main-title fw-bold mb-0">🎬 Mes Listes</h1>

				<p class="text-white mb-2">Gérez vos collections de films et séries préférés.</p>
			</div>

			<a href="{{ path('app_movie_list_new') }}" class="btn btn-primary shadow-sm">
				<i class="fas fa-plus me-2"></i>Créer une nouvelle liste
			</a>
		</div>

		{# Contenu principal #}
		{% if movie_lists is not empty %}
			<div class="row g-4">
				{% for list in movie_lists %}
					<div class="col-md-6 col-lg-4">
						<div class="card bg-dark h-100 border-0 shadow-sm hover-shadow transition-all">
							<div class="card-body d-flex flex-column justify-content-between">
								<div>
									<h5 class="card-title mb-2">
										<i class="fas fa-folder-open me-2"></i>
										{{ list.name }}
									</h5>
									{% if list.description %}
										<p class="text-light small mb-3">{{ list.description }}</p>
									{% endif %}
								</div>

								<div class="mt-auto">
									<a href="{{ path('app_movie_list_show', {'id': list.id}) }}" class="btn btn-outline-light btn-sm w-100">
										<i class="fas fa-eye me-1"></i>
										Voir la liste
									</a>

								</div>
							</div>
						</div>
					</div>
				{% endfor %}
			</div>
		{% else %}
			<div class="text-center py-5">
				<i class="fas fa-list fa-3x text-secondary mb-3"></i>
				<p class="lead text-muted mb-4">
					Vous n’avez encore créé aucune liste.
				</p>
				<a href="{{ path('app_movie_list_new') }}" class="btn btn-primary">
					<i class="fas fa-plus me-1"></i>Créer ma première liste
				</a>
			</div>
		{% endif %}
	</div>

{% endblock %}
